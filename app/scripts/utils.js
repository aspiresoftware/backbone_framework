/**
 * @desc        Common helper functions/utilities
 *
 */
require(['scripts/app'], function(){
    var utils = {
        /*Can have more than 1 functions to be initlized to perform different tasks togather.*/

    };
    return utils;
    console.log("from utils");
});